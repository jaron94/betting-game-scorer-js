# Pooled Neon connection string. Add the same value in Vercel.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional but recommended. When set, saving a finished game requires this code.
SCORER_ACCESS_CODE=choose-a-long-random-code
